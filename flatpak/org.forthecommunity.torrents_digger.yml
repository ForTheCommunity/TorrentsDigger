# Generated by flatpak-flutter v0.13.0 from flatpak-flutter.yml, do not edit
# Visit the project at https://github.com/TheAppgineer/flatpak-flutter
id: org.forthecommunity.torrents_digger
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: torrents_digger
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: torrents_digger
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin:/var/lib/rustup/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        PUB_CACHE: /run/build/torrents_digger/.pub-cache
        CARGO_HOME: /run/build/torrents_digger/cargo
        RUSTUP_HOME: /var/lib/rustup
    build-commands:
      - setup-flutter.sh
      - flutter build linux --release --no-pub
      - install -D $BUNDLE_PATH/torrents_digger /app/bin/torrents_digger
      - cp -r $BUNDLE_PATH/lib /app/bin/lib
      - cp -r $BUNDLE_PATH/data /app/bin/data
    sources:
      - type: git
        url: https://gitlab.com/ForTheCommunity/torrentsdigger.git
        commit: 5b9a3959955240384ae5d3ce737ca485e5c7b3b2
      - generated/sources/cargo.json
      - generated/sources/pubspec.json
    modules:
      - generated/modules/rustup-1.93.0.json
      - generated/modules/flutter-sdk-3.38.9.json
